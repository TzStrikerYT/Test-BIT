export interface Course {
    dayTime: string
    description: string
    duration: string
    endDate: string
    name: string
    place: string
    startDate: string
    updatedAt?: string
    _id?: string
    tag?: string
}